apiVersion: v1
kind: Service
metadata:
  name: fitflow-backend-service
  namespace: fitflow
  labels:
    app: fitflow-backend
spec:
  type: ClusterIP
  selector:
    app: fitflow-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000