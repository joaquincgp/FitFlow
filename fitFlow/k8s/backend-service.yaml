apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: fitflow-backend
spec:
  type: ClusterIP
  selector:
    app: fitflow-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
