apiVersion: v1
kind: Service
metadata:
  name: fitflow-frontend-service
  namespace: fitflow
  labels:
    app: fitflow-frontend
spec:
  type: NodePort
  selector:
    app: fitflow-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080